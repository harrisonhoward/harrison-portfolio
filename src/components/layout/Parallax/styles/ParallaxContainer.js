import { styled } from "@mui/material";
const ParallaxContainer = styled("div")({
    position: "fixed",
    left: 0,
    top: 0,
    height: "100vh",
    width: "100vw",
    overflow: "hidden",
    zIndex: 0,
});
export default ParallaxContainer;
